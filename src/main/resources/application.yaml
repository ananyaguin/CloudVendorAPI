spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cloud_vendor?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: An@ny@613M

  jpa:
    hibernate:
      ddl-auto: update
      management:
        endpoints:
          web:
            exposure:
              include: "*"

      dummyService:
        address: localhost
        port: 3306

